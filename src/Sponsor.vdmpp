class Sponsor

	types
		public SponsorName = seq of char;

	instance variables
		public name: SponsorName;
	
	operations
		public Sponsor(n: seq of char) res: Sponsor == (
			name := n
		)
		pre len n > 2
		post res.name = n;
	
end Sponsor