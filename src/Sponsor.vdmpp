class Sponsor

	types
		public String = seq1 of char;

	instance variables
		public name: String;
	
	operations
		public Sponsor(n: String) res: Sponsor == (
			name := n;
		)
		pre len n > 2
		post res.name = n;
	
end Sponsor